window.DMVAULT_FEEDBACK_CONFIG = Object.freeze({
  // 部署 Google Apps Script 網頁應用程式後，把 /exec 網址貼在引號內。
  endpoint: '',
  enabled: true,
  minMessageLength: 5,
  maxMessageLength: 2000,
  maxQueueSize: 100
});
